-- Пользователь снял галочку «Отправить в облако Insapp» при сохранении встречи.
-- 0 = можно отправлять на сервер (дефолт), 1 = встреча остаётся только локально.
-- Источник истины для всех путей загрузки (стоп записи, правка названия/типа,
-- AI-резюме): пока флаг = 1, insapp_upload_meeting_by_id не отправляет встречу.
-- Старые встречи получают 0 (поведение не меняется).
ALTER TABLE meetings ADD COLUMN cloud_opt_out INTEGER NOT NULL DEFAULT 0;
